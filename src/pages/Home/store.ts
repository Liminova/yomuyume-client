import { ref } from "vue";

const coverHeight = ref(300);
const recommendsContainerHeight = 500;
const gapPixel = 18;
const snackbarMessage = ref("");

export { coverHeight, recommendsContainerHeight, gapPixel, snackbarMessage };
